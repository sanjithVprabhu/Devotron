[project]
name = "veda-cap-broadcast"
version = "0.0.1"
requires-python = ">=3.11"
dependencies = ["veda-shared"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["veda_cap_broadcast"]

[tool.uv.sources]
veda-shared = { workspace = true }
