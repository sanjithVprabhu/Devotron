[project]
name = "veda-cap-recommendations"
version = "0.0.1"
requires-python = ">=3.11"
dependencies = ["veda-shared", "veda-llm-router"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["veda_cap_recommendations"]

[tool.uv.sources]
veda-shared = { workspace = true }
veda-llm-router = { workspace = true }
